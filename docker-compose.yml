version: "3"
services:
  app:
    container_name: node_app
    build: .
    ports:
      - "3000:3000"
    restart: always
    volumes:
      - ./:/app
      - node_modules:/app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
volumes:
  node_modules: